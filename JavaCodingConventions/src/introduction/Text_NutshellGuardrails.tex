\begin{enumerate}[nosep]\pslabel{lst:NutshellGuardrails}
	\item{Beautiful is better than ugly.}\pslabel{lst:ZoP:BeautifulVsUgly}
	\item{Explicit is better than implicit, and verbosity is your friend.}\pslabel{lst:ZoP:ExplicitVsImplicit}
	\item{Do not repeat yourself.}\pslabel{lst:ZoP:DRY}\index{DRY Principle}
	\item{Simple is better than complex.}\pslabel{lst:ZoP:SimpleVsComplex}
	\item{Complex is better than complicated.}\pslabel{lst:ZoP:ComplexVsComplicated}
	\item{Flat is better than nested.}\pslabel{lst:ZoP:FlatVsNested}
	\item{Sparse is better than dense.}\pslabel{lst:ZoP:SparseVsDense}
	\item{Readability counts.}\pslabel{lst:ZoP:Readablity}
	\item{Special cases aren't special enough to break the rules~…}\pslabel{lst:ZoP:SpecialCases}
	\item{…~although practicality beats purity.}\pslabel{lst:ZoP:Practicality}
	\item{Errors should never pass silently and Exceptions may never be swallowed~…}\pslabel{lst:ZoP:ErrorsMayNeverBeSwallowed}
	\item{…~unless \textit{explicitly} silenced.}\pslabel{lst:ZoP:ErrorsAreSilenced}
	\item{\textit{Either} log (and terminate) \textit{or} throw, neither both.}\pslabel{lst:ZoP:LogOrThrow}
	\item{In the face of ambiguity, refuse the temptation to guess.}\pslabel{lst:ZoP:DontGuess}
	\item{There should be one – and preferably only one – obvious way to do it~…}\pslabel{lst:ZoP:OneWay}
	\item{…~although that way may not be obvious at first.}
	\item{Now is better than never~…}\pslabel{lst:ZoP:Now}
	\item{…~although never is often better than \textit{right now}.}\pslabel{lst:ZoP:RightNow}
	\item{If the implementation is hard to explain, it's a bad idea.}\pslabel{lst:ZoP:BadIdea}
	\item{If the implementation is easy to explain, it may be a good idea.}\pslabel{lst:ZoP:GoodIdea}
	\item{Think about your future self and add a comment.}\pslabel{lst:ZoP:FutureSelf}
	\item{Without the unit tests\index{unit test}, the code is not complete~…}\pslabel{lst:ZoP:NotCompleteWithoutTest}
	\item{…~neither it will be complete without documentation.}\pslabel{lst:ZoP:NotCompleteWithoutDocumentation}
	\item{Always do it right in the first place!}\pslabel{lst:ZoP:BasicRule}
	\item{Have Fun!}\pslabel{lst:ZoP:HaveFun}
\end{enumerate}

%==============================================================================
% End of file!!
%==============================================================================
